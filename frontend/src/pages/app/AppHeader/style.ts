import { styled } from '@/stitches.config';

export const HeaderWrapper = styled('div', {
  padding: '12px 52px',
  width: '100%',
  height: '60px',
  display: 'flex',
  justifyContent: 'space-between',
});
