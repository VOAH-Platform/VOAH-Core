import { styled } from '@/stitches.config';

export const VoahSideWrapper = styled('div', {
  padding: '1.25rem 0 1.25rem 2rem',
  height: '100%',
  display: 'flex',
  justifyContent: 'flex-start',
  alignItems: 'stretch',
  gap: '1.25rem',
});
